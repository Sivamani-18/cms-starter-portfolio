[build]
  base    = "src"           # Optional: if your source code is in a specific directory like "src"
  publish = ".next"           # The directory to publish, generated by the build command
  command = "npm run build:staging"  # The command to build your project

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
